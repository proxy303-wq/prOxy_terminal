# PrOxy Handover — futures/warm/live day-1 (2026-09-07)

Read this first in a fresh chat.  Context: the account is LIVE for NIFTY only
(FINNIFTY + NIFTY-futures are PAPER, both gated).  Full ledger + decisions in
docs/V41_VALIDATION.md; earlier handover context in docs/HANDOVER.md §13-18.

## 1. CURRENT LIVE STATE (evening 07-Sep, ~23:00 IST)
- Service: proxy-terminal ACTIVE, workers: NIFTY (LIVE), FINNIFTY (paper),
  NIFTY-futures (paper, 2-lot budget).  BANKNIFTY DISABLED (loop + mode file gone).
- Account: equity ~₹7,02,xxx after the top-up to ~7L.  Modes: mode.json = live;
  NO mode_finnifty.json / mode_futures.json (=> paper; futures live also needs
  FUTURES_ALLOW_LIVE + the fill gate in run_futures_day - currently hard paper).
- Allocation (start.sh): NIFTY 0.40 / FINNIFTY 0.30 / FUTURES 0.30 of the full
  balance.  Lot standards: NIFTY 4, FINNIFTY 6 (ceiling; risk budget trims),
  futures margin-driven (1 on ~2.1L basis, cap MAX_LOTS 2) + FUTURES_PAPER_LOTS=2.
- Live profile knobs on the box == repo config (NO_STOP False, conf 65, ADX 18,
  arm 1.0, RSI 50/50, V4 delay 1, PE gate on, lots 4, DEFAULT ...).
- BRACKETS (super orders) ENABLED on the box (.env BRACKET_LIVE_ENABLED=1,
  BRACKET_ENTRY_STYLE=market) - all live entries are Dhan super orders
  (entry + resting target/SL, cancelled before every engine-side close).
- WARM-HISTORY default: BT_WARM_HISTORY=True in repo config; backtest engine
  carries indicator history across days + pre-seeds ~160 bars (live-worker parity).
  Set False only to reproduce legacy cold numbers.

## 2. THIS SESSION'S WORK (07-Sep + the 06-Sep night that set it up)
- Index-futures product: proxy/futures_config.py + proxy/futures_engine.py
  (paper engine LONG/SHORT in index points, own DB proxy_state_futures.sqlite,
  bracket live path), railway_worker --variant futures, streamlit "Futures" tab,
  Telegram /futures + button + CONFIRM-FUTURES-LIVE.  tools/_futures_* = the A/B
  + capture toolkit; tests/test_futures_engine.py (8 tests incl. bracket + tz).
- WARM default (user): backtest now warm by default.  Warm baselines:
  OPTION NIFTY TRAIN 1789tr/74.3%/+1,487,195/PF2.27, TEST 940tr/~81%/+446k
  (TEST conf census: >80 = 544tr/+284k, >90 = 209tr/+136k - reports/v41/
  confidence_nifty_warm_test.json).  FUTURES warm grid (reports/v41/
  v41_futures_warm_grid.json): stop5/arm1.0 best both windows (PF 4.98/5.99)
  - futures_config default.  ALL WARM NUMBERS = unvalidated ceilings; fills decide.
- NIFTY expiry roll (user): EXPIRY_ROLL_DAYS 3 (NIFTY; duals pinned 2),
  WEEKLY_EXPIRY_WEEKDAY 2 (Tuesday).  Verified live: Mon 07-Sep entries all on
  15SEP (next week), never the expiring 08SEP.
- Config unification: repo proxy/config.py aligned to the box's validated LIVE
  profile (was still data-mode - the 06-Sep near-miss where a full deploy would
  have opened live without stops).  Full tarball deploys are safe again.

## 3. LIVE DAY-1 (NIFTY, 07-Sep) - results + the reconciliation lesson
- 8 trades (4W/4L), engine-booked day P&L +1,606; REAL realized (Dhan positions)
  +3,542 pre-charges.  Real HIGHER than booked by ~+1,936.
- ROOT CAUSE FIXED (commit 4dd8398, deployed file-only - active at next worker
  restart): exit anchoring read the POSITION row's cumulative realizedProfit,
  which bundles same-strike repeats (24250-PE traded 3x -> leg2 booked -517 vs
  real +110.5; target leg booked at the REJECTED limit 423.76 while the market
  fallback filled 432.23 -> missed +1,309).  engine._anchor_exit_to_fill now
  anchors to the EXIT ORDER's own averageTradedPrice (new broker.get_order).
- Entry anchoring already real-fill based (log: "LIVE entry anchored to real
  fill 209.62").  All 8 today were brackets (journal [bracket] lines; /super/orders
  list is empty because legs are cancelled at each close - that is expected).

## 4. INCIDENTS + fixes this session
1. Box config.py was data-mode while modes=live (Sat deploy) -> restored the
   live profile (config.py.v41bak2) + unified repo config so it can't recur.
2. Box .env got corrupted (token glued onto the BRACKET_TRIGGER_OFFSET_PTS line
   by an env rewrite) -> repaired; tokens now come fresh from the daily push.
3. Futures engine mixed-tz crash (Dhan REST aware vs CSV naive times) -> all
   engine times flattened to naive IST (_naive_ist); regression test added.
4. FINNIFTY PAPER DAY-1 = NOTHING (see below) - a real feed bug, not a quiet day.

## 5. OPEN / NEXT-DAY RUNBOOK (Tue 08-Sep) - DO THESE FIRST
1. **FINNIFTY FEED BUG (highest priority):** on 07-Sep FINNIFTY's session logged
   "no bars received - session skipped".  Feed CONNECTED (09:15) + chain loaded
   (spot 26,056, 196 strikes) but the 5m BAR feed delivered NOTHING all day.
   Hypothesis: Dhan /v2/marketfeed/ltp IDX_I does not serve index 27 (FINNIFTY)
   the way it serves NIFTY 13 / BN 25.  At Tue open verify with a probe
   (fetch_ltp IDX_I 27 vs NSE_FNO underlying 26037 / charts API); fix the worker
   feed source, then paper day-1 re-runs for real.  Do NOT let FINNIFTY go live
   until it has traded paper bars.
2. **Spread capture was PARTIAL** (only 09:24-10:39, early-open window): median
   4.0pt / p25 2.0 / p75 5.9 (n=664).  NOT the full-day number.  Rerun the FULL
   session Tue (python tools/_futures_spread_capture.py from the Windows machine,
   ~09:15-15:30) -> reports/futures_spread_<date>.json = the futures fill-gate
   input.  Until then FUT_SLIPPAGE_PTS=1.0 stays a guess (probably LOW).
3. **Futures geometry flag from paper:** 7 paper trades on 07-Sep, net -1,265
   (1x TARGET +655, 2x STOP -840, 5x LOCK at -60).  The +1pt lock grosses 130 at
   2 lots but friction = 190 (slip 2x65 + fees 60) -> marginal locks net -60.
   Re-tune arm (1.5-2.0pt) once the measured spread is in hand; futures stays
   paper until fills + geometry survive.
4. Exit-anchor hardening activates at the next worker restart (Tue pre-market).
5. NIFTY day-2: watch that booked P&L now tracks real (anchor fix), brackets fill
   as expected, and the expiry roll holds (Tue = expiry day -> should trade 22SEP).

## 6. KEY NUMBERS TO REMEMBER (warm, unvalidated ceilings)
- NIFTY options warm: TRAIN PF 2.27 / TEST PF 2.61 (1789/940 tr).  ~17-21%/mo at
  the backtest basis; live basis is 0.5% risk -> small but consistent.
- Futures warm (engine, 1 lot): TRAIN 2636tr avgR~1.9 / TEST 1273tr avgR~2.1.
- FINNIFTY scout (other session): mid-model test PF ~2.7-2.8, real-scale unmeasured.
- Kelly-style sizing: only AFTER real fills prove per-class edge; ~1/4 Kelly as
  ceiling; 0.5% risk stays until then (fills = the gate, everything else is noise).

## 7. RULES THAT MUST NOT BE BROKEN
- NIFTY live; FINNIFTY + futures live ONLY via explicit flip (mode file + ALLOW
  env) AND after their paper/fill gates pass.  Never restart mid-session with an
  open live position.  Full tarball deploys only from a config-equal repo (now
  true).  Never commit .env / reports/ / data/ / .oracle.
- After ANY deploy that touches .env/box.env: run tools/_token_restore.py.
- Dashboard stays read-only; mode flips happen via Telegram CONFIRM flows only.
